spring.application.name=portfolio

# MySQL Database
spring.datasource.url=jdbc:mysql://${DB_HOST_PORT:localhost:3306}/${DB_NAME:portfolio_db}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Eakhal@2004}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Server Configuration
server.port=8080

# Logging
logging.level.org.springframework.security=INFO
logging.level.com.portfolio=DEBUG

# JWT Secret (This should be randomly generated and long enough, storing in properties for now)
jwt.secret=404E635266556A586E327235753878214125442A472D4B6150645367566B5970
jwt.expiration=86400000
